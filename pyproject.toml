[project]
name = "thyroidsynthesis"
version = "0.1"
description = "Steerable thyroid synthesis"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["thyroidsynthesis"]
